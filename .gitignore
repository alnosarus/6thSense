# --- Local data (never commit large assets) ---
data/incoming/
data/raw/
data/cache/
data/processed/
data/processed_full/

# Legacy dataset tree (deprecated; paths ignored if still present locally)
datasets/raw/
datasets/processed/
datasets/processed_full/

*.h5
*.hdf5
*.mat
*.npy
*.npz

# Large archives (e.g. dataset downloads — keep local only)
*.zip
*.7z
*.tar.gz

# Python
__pycache__/
*.pyc
.venv/
venv/
env/
.pytest_cache/
.coverage
htmlcov/
*.log

# Jupyter
.ipynb_checkpoints/

# Model outputs (ML weights/binaries – NOT the SQLAlchemy app models package)
models/
!backend/app/models/
runs/
*.pth
*.pt
*.onnx

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Cursor / local agent & GSD planning (keep on disk; do not publish)
.cursor/
.planning/

# Secrets
.env
.env.local
.env.*.local
!.env.example

# Local config overrides (optional)
config/senseprobe.local.yaml

# Node / frontend (Vite)
node_modules/
dist/
.vite/
frontend/dist/
frontend/node_modules/
landing/dist/
landing/node_modules/

# Superpowers design/plan docs — local only, not committed
docs/superpowers/

# Superpowers brainstorm visual companion session files — local only
.superpowers/
