# Optional: absolute path to large dataset archive (used by scripts/setup_data.py)
# SENSEPROBE_DATA_ZIP=/absolute/path/to/TUFFC_2022_Bi_Directional_elasto.zip

# Optional: config overrides (see config/senseprobe.defaults.yaml)
# SENSEPROBE_CONFIG=/absolute/path/to/senseprobe.local.yaml
# SENSEPROBE_DATASET=tuffc_2022
# SENSEPROBE_DATA_ROOT=data
# SENSEPROBE_RESULTS_DIR=results
# SENSEPROBE_MODELS_DIR=models
